import tasks from "./fake.api/tasks.api";
import taskBoards from "./fake.api/tasksBoadrs.api";
import cards from "./fake.api/cards.api";
import users from "./fake.api/users.api";

const api = {
  tasks,
  taskBoards,
  cards,
  users
};

export default api;
